---
exec: /usr/bin/apt
become:
  autoremove: autoremove
  deps: build-dep
  download: download
  fix: install -f
  install: install
  remove: remove
  update: update
  upgrade: upgrade
  sysupgrade: dist-upgrade
  builddep: build-dep
user:
  depends: depends
  info: show
  help: help
  search: search
  version: version
  installed: list --installed
